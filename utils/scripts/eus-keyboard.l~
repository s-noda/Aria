(ros::load-ros-manifest "roseus")

(ros::roseus "eus_keyboard")
(ros::advertise "/euslisp/keyboard" std_msgs/String)

(ros::rate 100)

(while (ros::ok)
  (setq key (read-line))
  (setq (instance std_msgs::string :init))
  (send msg :data key)
  (ros::publish "/euslisp/keyboard" msg)
  (ros::sleep))